{
  "_comment": [
    "Declarative record of the Cloudflare Pages project's runtime configuration.",
    "",
    "Why this file exists: Pages bindings are NOT covered by workers/wrangler.toml.",
    "That file configures the standalone Worker. Production serves /api/* from a",
    "_worker.js bundled into the Pages output (workers/src/pages-entry.ts), and that",
    "function reads its bindings from the PAGES PROJECT, which is configured through",
    "the dashboard or API and was therefore recorded nowhere.",
    "",
    "The consequence, observed in production on 2026-07-26: the project had no D1",
    "binding at all. Auth succeeded and /health returned 200, but every data route",
    "500'd with \"Cannot read properties of undefined (reading 'prepare')\" because",
    "c.env.DB was undefined. Nothing in the repo could have revealed that.",
    "",
    "Apply with:  node scripts/sync-pages-config.mjs",
    "Assert with: node scripts/sync-pages-config.mjs --check   (used by CI)",
    "",
    "Secret VALUES are deliberately absent — only the names are asserted, so this",
    "file is safe to commit."
  ],

  "projectName": "languagebuilder-frontend",

  "environments": ["production", "preview"],

  "d1_databases": {
    "DB": {
      "database_name": "languagebuilder",
      "id": "6216c466-c244-4c16-8569-c7281585fbc6"
    }
  },

  "r2_buckets": {
    "languagebuilder_assets": {
      "name": "languagebuilder-assets"
    }
  },

  "requiredEnvVarNames": ["ACCESS_TEAM_DOMAIN", "ACCESS_AUD"],

  "_accessNote": [
    "Cloudflare Access went live 2026-07-26. Setting BOTH of the above switches",
    "workers/src/index.ts from shared-token auth to per-user identity, so they are",
    "now required: dropping either one silently downgrades the whole site to a",
    "shared bearer token instead of failing loudly.",
    "",
    "Team domain: fjallouli.cloudflareaccess.com",
    "Access app:  languagebuilder (covers the host AND *.host for previews)",
    "",
    "API_TOKEN is no longer required. It is still set on the project, harmlessly —",
    "Access mode ignores the Authorization header — but it is no longer built into",
    "the frontend bundle, which is the point. Do not re-add it to this list; that",
    "would make a token-mode deployment pass the check.",
    "",
    "How this was misconfigured before, worth not repeating: the Access app had a",
    "bypass-everyone policy attached alongside a broken email rule, so the site",
    "returned 200 to anyone while looking configured in the dashboard."
  ]
}
