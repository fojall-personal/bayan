import { PatternGrid } from '@/components/vocabulary/PatternGrid';

export const metadata = {
  title: 'Root × pattern — Bayan',
  description: 'Which verb forms you know, crossed against your known roots.',
};

export default function PatternsPage() {
  return <PatternGrid />;
}
