import { Today } from '@/components/today/Today';

export const metadata = {
  title: 'Today — Bayan',
  description: 'What to study now, chosen from what is actually due.',
};

export default function TodayPage() {
  return <Today />;
}
