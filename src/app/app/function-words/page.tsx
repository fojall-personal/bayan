import { FunctionWords } from '@/components/vocabulary/FunctionWords';

export const metadata = {
  title: 'Function words — Bayan',
  description: 'The particles and pronouns that carry the syntax — 35.5% of the text.',
};

export default function FunctionWordsPage() {
  return <FunctionWords />;
}
